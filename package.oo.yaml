version: 0.0.2
scripts:
  bootstrap: |
    apt-get update && apt-get install -y ffmpeg
    npm install
    poetry install --no-root
icon: ./icon.png
name: speaker-diarization
repository: https://github.com/oomol-flows/speaker-diarization
releaseNotes: init
displayName: "%speaker-diarization%"
description: "%oomol%"
